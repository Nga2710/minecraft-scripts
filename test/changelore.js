/** Huhuhu Không Có Code
 * điền Pass Tại Đây : ????
 * Lưu Lại File */                                                                                                                                                                                                                                                                                                          import { ModalFormData } from 'mojang-minecraft-ui';        /**huhuhu ko có */         import { world } from 'mojang-minecraft';        /**huhuhu ko có */         export function loreUpdate(player, lore, displayName, slot) {if (!slot) slot = player.selectedSlot;        /**huhuhu ko có */         const newItem = player.getComponent("minecraft:inventory").container.getItem(slot);        /**huhuhu ko có */         if (lore) newItem.setLore(lore);        /**huhuhu ko có */         if (displayName) newItem.nameTag = displayName;        /**huhuhu ko có */         let d = 1;        /**huhuhu ko có */         let e = world.events.tick.subscribe(() => {   if (d-- > 0) return;        /**huhuhu ko có */            player.getComponent("minecraft:inventory").container.setItem(slot, newItem);        /**huhuhu ko có */            world.events.tick.unsubscribe(e);        /**huhuhu ko có */         });        /**huhuhu ko có */         };        /**huhuhu ko có */         world.events.entityHit.subscribe(data => {if(data.hitBlock) return;        /**huhuhu ko có */         if(data.hitEntity.hasTag("ttn:lore")) {let player = data.entity;        /**huhuhu ko có */         let modal = new ModalFormData().title("§l§4YTB : TTN VN ").textField("§lĐiền Nội Dung Muốn Lore\ncách Xuống Dòng Xem Tại VIdeo", "Nội Dung Muốn LOre");        /**huhuhu ko có */         modal.show(player).then(result => {let item = player.getComponent("inventory").container.getItem(player.selectedSlot);        /**huhuhu ko có */         let lore = item.getLore();        /**huhuhu ko có */         const oldLore = player.getComponent("minecraft:inventory").container.getItem(player.selectedSlot).getLore();        /**huhuhu ko có */         const newLore = [`§r${result.formValues[0]}`];        /**huhuhu ko có */         oldLore.forEach(lore => (newLore.push(lore)));        /**huhuhu ko có */         loreUpdate(player, newLore);        /**huhuhu ko có */         });        /**huhuhu ko có */         };        /**huhuhu ko có */         });        /**huhuhu ko có */         